
  # EchoKey_EchoKey (Copy)

  This is a code bundle for EchoKey_EchoKey (Copy). The original project is available at https://www.figma.com/design/TfcFFeWsvDETPaoBg514uo/EchoKey_EchoKey--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  